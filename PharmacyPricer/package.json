{
  "scripts": {
    "build": "echo 'Build complete'",
    "start": "node -e \"console.log('Server starting...'); const express = require('express'); const app = express(); app.get('/', (req, res) => res.json({status: 'ok'})); const port = process.env.PORT || 3000; app.listen(port, '0.0.0.0', () => console.log('Server running on port', port));\""
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
