{
  "name": "pharmcost-pro",
  "version": "1.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "echo 'Backend-only build' && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
    "start": "NODE_ENV=production node dist/index.js",
    "preinstall": "rm -f package-lock.json"
  },
  "dependencies": {
    "express": "4.21.2",
    "esbuild": "0.25.0",
    "typescript": "5.6.3"
  }
}
